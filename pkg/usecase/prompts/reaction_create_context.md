This case was raised by an emoji reaction on a single Slack message — the anchor, timestamp {{ .AnchorTS }}. Someone reacting flagged that message as worth tracking, but a single message rarely tells the whole story.

Read the messages BEFORE and AFTER the anchor in the conversation provided to you, and work out what is actually going on: the request being made, the incident being reported, or the decision the anchor points at. Do NOT build the case from the anchored message alone, in isolation.

Base the title, the description, and every field on the full surrounding context — not just the single reacted line.
