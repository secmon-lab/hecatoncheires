package job

// RenderReflectInstructionForTest exposes renderReflectInstruction so the
// external test package can golden-assert the reflection instruction template.
var RenderReflectInstructionForTest = renderReflectInstruction
